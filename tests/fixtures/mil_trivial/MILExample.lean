import Mathlib

/-! A trivial exercise in the style of *Mathematics in Lean* (C02, "Calculating"):
rewrite a product. Stated with `sorry` so the platform has something to complete. -/

theorem mul_comm_assoc (a b c : ℝ) : a * b * c = b * (a * c) := by
  sorry
